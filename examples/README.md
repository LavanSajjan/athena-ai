# Athena AI Examples

This directory contains runnable examples demonstrating the current Athena AI
data platform.

The examples use the existing production components without introducing
additional application abstractions.

## Prerequisites

From the repository root:

```powershell
uv sync